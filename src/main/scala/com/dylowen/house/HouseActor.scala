package com.dylowen.house

/**
  * TODO add description
  *
  * @author dylan.owen
  * @since Aug-2018
  */
object HouseActor {

  /*
  val behavior: Behavior[NotUsed] = Actor.supervise(
    Actor.deferred((prestart: ActorContext[NotUsed]) => {



      Actor.immutable((ctx: ActorContext[NotUsed], _: NotUsed) => {

        Actor.same
      })
    })
  ).onFailure(SupervisorStrategy.restart)
  */
}
